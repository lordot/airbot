version: '3.11'

services:
 db:
  image: postgres:13.0-alpine
  volumes:
   - postgres_db:/var/lib/postgresql/data/
  env_file:
    - .env
  healthcheck:
   test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]

 scraper:
  image: "165.22.80.137:8083/airscraper:1.0"
  restart: always
  ports:
   - "8000:8000"

 django:
  image: "165.22.80.137:8083/airbot:1.1.0"
  restart: always
  env_file:
   - .env
  volumes:
   - static_value:/app/static/
  depends_on:
   db:
    condition: service_healthy

 nginx:
  image: "165.22.80.137:8083/airnginx:1.1.0"
  ports:
   - "80:80"
  volumes:
   - static_value:/var/html/static/
  depends_on:
   - django

volumes:
 static_value:
 postgres_db:
